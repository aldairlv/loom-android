package com.loom.core.database.model

/*
import androidx.room.ColumnInfo
import androidx.room.Entity
import androidx.room.PrimaryKey

@Entity(tableName = "timeline_metadata")
data class TimelineMetadataEntity(
    @PrimaryKey
    @ColumnInfo(name = "timeline_type")
    val timelineType: String, // El nombre de la categoría de timeline (por ejemplo, "dashboard")

    @ColumnInfo(name = "cursor")
    val cursor: String?,

    @ColumnInfo(name = "last_updated")
    val lastUpdated: Long
)
*/
