package com.loom.core.database.dao
/*
import androidx.room.Dao
import androidx.room.Query
import androidx.room.Upsert
import com.loom.core.database.model.TimelineMetadataEntity

@Dao
interface TimelineMetadataDao {
    @Query("SELECT * FROM timeline_metadata WHERE timeline_type = :type")
    suspend fun getMetadata(type: String): TimelineMetadataEntity?

    @Upsert
    suspend fun upsertMetadata(metadata: TimelineMetadataEntity)

}
*/