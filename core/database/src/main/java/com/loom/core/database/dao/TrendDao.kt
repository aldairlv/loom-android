package com.loom.core.database.dao

/*
import androidx.room.Dao
import androidx.room.Query
import androidx.room.Transaction
import androidx.room.Upsert
import com.loom.core.database.model.TrendEntity
import com.loom.core.database.model.TrendItemEntity
import kotlinx.coroutines.flow.Flow

@Dao
interface TrendDao {

    @Upsert
    suspend fun upsertTrendItems(
        items: List<TrendItemEntity>
    )

    @Upsert
    suspend fun upsertTrend(
        item: TrendEntity
    )


    @Upsert
    suspend fun upsertTrends(trends: List<TrendEntity>)
}
*/
